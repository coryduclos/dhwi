<h1>Your choices...</h1>
<link href='http://fonts.googleapis.com/css?family=Ribeye' rel='stylesheet' type='text/css'>
<table cellpadding="0" cellspacing="0">
  <thead>
    <tr>
      <th><th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td><%= link_to topic.title, topic %></td>
        <td><%= pluralize(topic.votes.length, "vote")%></td>
        <td><%= button_to '+1', votes_path(topic_id: topic.id), method: :post %></td>
        <td><%= link_to 'Delete', topic, :confirm => 'Are you sure?', :method => :delete %></td>
      </tr>
    <% end %>
  </tobody>
</table>

<br />

<%= link_to 'Are we missing a great movie? Add it in!', new_topic_path %>
